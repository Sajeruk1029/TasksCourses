#ifndef EIGHTMODULE
#define EIGHTMODULE

	#include <math.h>

	typedef struct
	{
		double rootOne;
		double rootTwo;

	} RootsOfEquation2;

	RootsOfEquation2 solutionOfEquation2(double, double, double, unsigned char);

#endif
