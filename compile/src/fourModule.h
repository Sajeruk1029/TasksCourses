#ifndef FOURMODULE
#define FOURMODULE

	#include <math.h>

	typedef struct
	{
		double rootOne;
		double rootTwo;

	} RootsOfEquation;

	RootsOfEquation solutionOfEquation(double, double, double);

#endif
